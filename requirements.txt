# Djinn — dependencies
# Prefer `uv sync` (uses pyproject.toml + uv.lock). This file is the pip path.

# Voice input: VAD + speech-to-text
faster-whisper>=1.1.0
silero-vad>=5.1

# Voice output: local TTS, with a cloud fallback
kokoro-onnx>=0.4.0
onnxruntime>=1.17.0
edge-tts>=6.1.0

# Audio I/O
sounddevice>=0.5.0
soundfile>=0.12.0
numpy>=2.0.2

# Brain: Gemini via Vertex AI or AI Studio.
# NB: google-generativeai is discontinued; google-genai replaces it and is the
# only one that speaks to Vertex.
google-genai>=2.11.0

# Memory & config
pyyaml>=6.0
peewee>=3.17.0
python-dotenv>=1.0.1

# System
keyboard>=0.13.5
